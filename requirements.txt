fastapi==0.117.1
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
titiler.core==0.18.0
aiofiles
PyJWT==2.8.0

# `titiler.mosaic` y `boto3` se fueron el 2026-09-24 con el router /mosaic
# (`DECISIONS #64` del worker): eran sus únicas dos razones de existir. boto3 lo
# pedía `cogeo_mosaic.backends.s3.S3Backend`; GDAL nunca lo usó, llega a MinIO
# por sus propias variables.

# Lo usa `terra_tiles/health.py` para sondear MinIO en /health/ready, y también
# scripts/check_titiler_minio.py. Era solo del script hasta que el readiness
# check pasó a depender de él: si se saca, /health/ready revienta al importar.
minio==7.2.16

# --- Solo para scripts/check_titiler_minio.py (diagnóstico, no es parte del servicio) ---
numpy
